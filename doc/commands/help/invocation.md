`=help` - Same thing as `=list-commands`.
`=help command` - Replies with `command`'s description **and** invocation.
`=help command <description OR invocation>` - Replies with the corresponding resource for `command`